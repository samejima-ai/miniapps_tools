# CaaF gen-2 — L0 着手 献上（2026-06-07）

L0 spec-architect セッション。汎用FW v1.0.0 への本格着手。AI 能力: claude-opus-4-8。persona: sheep-navigator。

## やったこと

1. **振り返り儀式（レベル2）**: M0 仮運用までの文脈と照合。gen-1 の「ドメイン非依存化」意図と v1.0.0 は矛盾せず、その深化と確認。
2. **L0 対話（4 論点を人間確定）**: 全面書き換え / フル v1.0.0 / 別ワークスペース抽出 / 当面工具のみ。→ ADR-007。
3. **`@caaf/core` スキャフォールド**: 純ロジック層（types/validate/resolver/signal/guards/router 決定/intent 正規化/execute）を実装。LLM/DB を伴う層は注入インターフェース（Adapter/IntentClassifier/Extractor/RouterScorer）として定義し Core純度を保持。
4. **仕様・移行ドキュメント**: package SPEC.md（v1.0.0 自己完結 + 実装対応表）/ README / `spec/caaf-migration.md`（gen-1↔gen-2 対応・工具 binding・ワークスペース化手順・M-A〜M-F ロードマップ）/ gen-1 doc superseded マーク。
5. **ガバナンス更新**: ADR-007 / REGIME.md（current_focus・LC=1・モード再判定 M2 維持）。

## 生成物

| パス | 内容 |
|---|---|
| `packages/caaf-core/{package.json,tsconfig.json,vitest.config.ts,.gitignore}` | 自己完結パッケージ（外部依存ゼロ） |
| `packages/caaf-core/src/*.ts` | Core 11 モジュール（§13 対応表） |
| `packages/caaf-core/test/*.test.ts` | 純ロジック unit test（vitest、CI 用） |
| `packages/caaf-core/scripts/purity-check.mjs` | Core純度 grep 検証 |
| `packages/caaf-core/{SPEC.md,README.md}` | FW v1.0.0 自己完結仕様 + 使い方 |
| `spec/caaf-migration.md` | 移行計画 + 工具 binding + ロードマップ |
| `spec/caaf-component.md` | gen-1 superseded マーク追記 |
| `app/history/ARCH-DECISIONS.md` | ADR-007 追記 |
| `app/REGIME.md` | current_focus / LC / gen-2 再判定 |

## 自己検証（L0 §7.4）

- [x] **broken reference**: 生成ドキュメントの相互参照（package SPEC ↔ migration ↔ README ↔ ADR ↔ REGIME）の相対パスが実体と一致（下記 grep で確認）。
- [x] **scaffold smoke test**: `tsc --noEmit -p packages/caaf-core`（install 不要）= PASS。さらに dist へ emit し node で **runtime smoke**（validate/resolver/guards/foreign-strip/router/signal/intent）を実行 = PASS。`pnpm purity` = PASS。
  - vitest 本体の実行は `pnpm install` 必須のため L0 環境では未実行（CI/L1 で実行）。同等のランタイム検証は上記 node smoke でカバー済。
- [x] **DONT 自己照合**: 入力レイヤーのみの書き換えで `item_movements`/migration/View/RLS 不変。罠A・D-1〜D-12 を `spec/caaf-migration.md` §7 で逐項維持。SPEC に DONT 違反記述なし。Core は固有名詞・外部依存ゼロ（purity 緑）。
- [x] **Pre-flight 充足**: ritual-protocol / persona-spec を読了の上で儀式・persona を適用。
- [x] **受け入れ基準**: ドキュメント生成 + scaffold 実体 + smoke PASS + 本 §7.4 PASS。

## 既知の保留（L1 へ）

- root `pnpm-workspace.yaml` は L0 で未作成（稼働中 M0 アプリの Vercel ビルド経路保護のため）。L1 M-A でローカル検証付きで適用（`spec/caaf-migration.md` §5）。
- LLM 注入実装・db Adapter・host UI・gen-1 撤去は M-B〜M-F。

## L1 への引き継ぎ

`spec/caaf-migration.md` §6 のマイルストン M-A から着手。各 M は独立レビュー可能単位。M-F（gen-1 撤去）はパリティ検証通過後のみ。
